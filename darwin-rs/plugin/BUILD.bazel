load("//:defs.bzl", "darwin_code_rust_crate")

darwin_code_rust_crate(
    name = "plugin",
    crate_name = "darwin_code_plugin",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
